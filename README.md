# peeling-machine
Peeling Machine is a developer tool for capturing and inspecting local HTTP/HTTPS network traffic. Operating as a local proxy with its own custom Certificate Authority (CA), it seamlessly intercepts, decrypts, and logs requests from browsers and mobile devices for easy API debugging and network analysis.
