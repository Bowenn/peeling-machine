node_modules
*.log
.DS_Store
# Only pnpm is used here — reject other package managers' lockfiles.
package-lock.json
yarn.lock
# Ignore everything in dist/ except the committed stub. The real build
# output (assets/, hashed JS/CSS) is regenerated by `npm run build`.
dist/*
!dist/index.html
